@model List<Bloomfiy.Models.CartItem>
@{
    ViewBag.Title = "Shopping Cart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<section class="cart-page container">
    <h2>Shopping Cart</h2>

    @if (Model == null || !Model.Any())
    {
        <p>Your cart is empty.</p>
    }
</section>

